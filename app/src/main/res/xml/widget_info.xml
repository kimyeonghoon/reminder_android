<?xml version="1.0" encoding="utf-8"?>
<!-- Widget configuration for reminder list widget
     - targetCellWidth/targetCellHeight: API 31+ attributes for better sizing (ignored on older versions)
     - 4x2 size: wider and shorter for better landscape fit
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="120dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_reminder_list"
    android:description="@string/widget_description"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewImage="@android:drawable/ic_menu_view" />
